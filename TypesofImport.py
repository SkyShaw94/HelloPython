import statistics
import statistics as s #We don't have to type 'statistics' just 's' will do eg; s.variance
from statistics import variance # Reference directly to variance(x) statistics.variance(x) not needed
from statistics import variance , mean
from statistics import variance as v , mean as m
from statistics import * # Regex that imports all the funtion can directly use them
